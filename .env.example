# Flask Configuration
SECRET_KEY=your-super-secret-key-here

# MongoDB Atlas Database URI
# Replace <db_password> with your actual password and <cluster-url> with your cluster
MONGO_URI=mongodb+srv://<username>:<db_password>@<cluster-url>/?retryWrites=true&w=majority
